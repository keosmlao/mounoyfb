# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/src/generated/prisma

# ໄຟລ໌ຕົວຢ່າງ env ຕ້ອງ commit (ຂໍ້ຂ້າງເທິງ .env* ຈັບມັນໄປນຳ)
!.env.example

# skill ຂອງ Prisma ທີ່ຕິດຕັ້ງອັດຕະໂນມັດ — ສ້າງຄືນໄດ້ຈາກ skills-lock.json
/.agents/skills
/.claude/skills
/.windsurf/skills

# ສຳຮອງຖານຂໍ້ມູນ — ມີ Facebook token ຢູ່ຂ້າງໃນ ຫ້າມ push
/deploy/dump/
